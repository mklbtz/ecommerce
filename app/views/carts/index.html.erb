<h1>Listing carts</h1>

<table>
  <tr>
    <th>Customer</th>
    <th>Items</th>
    <th>Quantity</th>
  </tr>

<% @carts.each do |cart| %>
  <tr>
    <td><%=h cart.customer %></td>
    <td><%=h cart.items %></td>
    <td><%=h cart.quantity %></td>
    <td><%= link_to 'Show', cart %></td>
    <td><%= link_to 'Edit', edit_cart_path(cart) %></td>
    <td><%= link_to 'Destroy', cart, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New cart', new_cart_path %>