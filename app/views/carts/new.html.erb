<h1>New cart</h1>

<% form_for(@cart) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :has_many %><br />
    <%= f.text_field :has_many %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', carts_path %>